# ShopSphere Frontend

React + TypeScript + Vite foundation for the ShopSphere monorepo.

## Scripts

```bash
npm install
npm run dev
npm run build
```

Dev server: `http://localhost:5173`  
API calls to `/api/*` are proxied to `http://localhost:8080` during development.
